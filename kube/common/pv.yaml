apiVersion: v1
kind: PersistentVolume
metadata:
  name: laravel-in-kubernetes-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/paul/pv"
